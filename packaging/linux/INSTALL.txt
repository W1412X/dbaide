DBAide — Linux install notes
=============================

Extract this folder anywhere, then run:

  ./DBAide/DBAide

Minimum supported platform: Ubuntu 20.04 LTS (glibc 2.31) and comparable
Debian-based desktops. Builds are produced on Ubuntu 20.04 so newer Ubuntu /
Debian releases can run the same tarball; older releases (18.04 and below) are
not supported.

Recent releases bundle the Qt xcb libraries (including libxcb-cursor) needed on
Ubuntu 20.04+. No extra packages should be required for the frozen app.

If you still see "Could not load the Qt platform plugin xcb":

  sudo apt update
  sudo apt install -y libxcb-cursor0 libxkbcommon-x11-0 libgl1 libegl1

On Ubuntu 20.04, enable universe first if libxcb-cursor0 is not found:

  sudo add-apt-repository universe
  sudo apt update

For the pip / source install (dbaide-gui), install the same packages once.

Optional desktop entry:

  cp DBAide/dbaide.desktop ~/.local/share/applications/
  # Edit Exec= path inside the .desktop file to your extract location.
